{
    "scripts": {
        "setup": "cp $CONDUCTOR_ROOT_PATH/.env .env && pnpm install",
        "run": "pnpm prisma migrate dev && pnpm prisma generate && pnpm db:seed && pnpm run dev",
        "archive": ""
    }
}
